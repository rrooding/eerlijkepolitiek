- if user_authenticated?
  .authenticated
    Name:
    .user-name= current_user.first_name
    = button_to 'logout', logout_path, class: 'logout', method: :delete
- else
  = link_to 'login', login_path, class: 'js-login'
